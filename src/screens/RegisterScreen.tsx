import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

import { palette } from '../styles';

const RegisterScreen = () => {
  return (
    <View>
      <Text style={{ color: palette.blackColor }}>Login screen</Text>
    </View>
  );
};

export default RegisterScreen;
